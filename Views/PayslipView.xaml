<UserControl x:Class="PayrollSystem.Views.PayslipView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32,28,32,32">

            <!-- ═══ PAYSLIP DOCUMENT ═══ -->
            <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="40" Margin="0">
                <StackPanel>
                    <!-- Top Header -->
                    <StackPanel HorizontalAlignment="Center" Margin="0,0,0,25">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="🏫" Foreground="{DynamicResource PrimaryBrush}" FontSize="26" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding CompanyName}" Foreground="{DynamicResource PrimaryBrush}" FontSize="24" FontWeight="Bold"/>
                        </StackPanel>
                        <TextBlock Text="{Binding CompanyAddress}" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" HorizontalAlignment="Center" Margin="0,5,0,10"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="Payslip for the Period of " Foreground="{DynamicResource TextSecondaryBrush}" FontSize="13"/>
                            <TextBlock Text="{Binding PeriodText}" Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold" FontSize="13"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Employee Info Table -->
                    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Margin="0,0,0,25">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="15,12">
                                <TextBlock Text="👤" Foreground="{DynamicResource PrimaryBrush}" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="EMPLOYEE INFORMATION" Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold" FontSize="12" VerticalAlignment="Center"/>
                            </StackPanel>
                            <Border Height="1" Background="{DynamicResource BorderBrush}"/>
                            <Grid Margin="0,10,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Employee Name:" Foreground="{DynamicResource TextPrimaryBrush}" Padding="15,6"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding EmployeeName}" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold" Padding="5,6"/>
                                
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Employee ID:" Foreground="{DynamicResource TextPrimaryBrush}" Padding="15,6"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding EmpNumber}" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold" Padding="5,6"/>
                                
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Position:" Foreground="{DynamicResource TextPrimaryBrush}" Padding="15,6"/>
                                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Position}" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold" Padding="5,6"/>

                                <TextBlock Grid.Row="3" Grid.Column="0" Text="Pay Period:" Foreground="{DynamicResource TextPrimaryBrush}" Padding="15,6"/>
                                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding PeriodText}" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold" Padding="5,6"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Main Grid Split -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- LEFT: Earnings Table -->
                        <Border Grid.Column="0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="15,12">
                                    <TextBlock Text="💵" Foreground="{DynamicResource PrimaryBrush}" FontSize="14" Margin="0,0,8,0"/>
                                    <TextBlock Text="EARNINGS" Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold" FontSize="12" VerticalAlignment="Center"/>
                                </StackPanel>
                                <Border Grid.Row="0" Height="1" Background="{DynamicResource BorderBrush}" VerticalAlignment="Bottom"/>
                                
                                <Grid Grid.Row="1" Margin="15,10,15,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2.5*"/>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="1.5*"/>
                                        <ColumnDefinition Width="2*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Description" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                                    <TextBlock Grid.Column="1" Text="Rate Per Day" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold" TextAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="Days/Hrs" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold" TextAlignment="Center"/>
                                    <TextBlock Grid.Column="3" Text="Amount" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold" TextAlignment="Right"/>
                                </Grid>

                                <StackPanel Grid.Row="2" Margin="15,5,15,15">
                                    <StackPanel.Resources>
                                        <Style TargetType="Grid">
                                            <Setter Property="Margin" Value="0,6"/>
                                        </Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                                        </Style>
                                    </StackPanel.Resources>

                                    <!-- Data Rows -->
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="2.5*"/><ColumnDefinition Width="2*"/><ColumnDefinition Width="1.5*"/><ColumnDefinition Width="2*"/></Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Basic Salary"/>
                                        <TextBlock Grid.Column="1" Text="—" TextAlignment="Center"/>
                                        <TextBlock Grid.Column="2" Text="{Binding WorkDays}" TextAlignment="Center"/>
                                        <TextBlock Grid.Column="3" Text="{Binding BasicSalary, StringFormat=₱ {0}}" TextAlignment="Right" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    </Grid>
                                    
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="2.5*"/><ColumnDefinition Width="2*"/><ColumnDefinition Width="1.5*"/><ColumnDefinition Width="2*"/></Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Overtime Pay"/>
                                        <TextBlock Grid.Column="1" Text="—" TextAlignment="Center"/>
                                        <TextBlock Grid.Column="2" Text="{Binding OvertimeHours}" TextAlignment="Center"/>
                                        <TextBlock Grid.Column="3" Text="{Binding OtPay, StringFormat=₱ {0}}" TextAlignment="Right" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    </Grid>

                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="2.5*"/><ColumnDefinition Width="2*"/><ColumnDefinition Width="1.5*"/><ColumnDefinition Width="2*"/></Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Holiday Pay"/>
                                        <TextBlock Grid.Column="1" Text="—" TextAlignment="Center"/>
                                        <TextBlock Grid.Column="2" Text="{Binding HolidayHours}" TextAlignment="Center"/>
                                        <TextBlock Grid.Column="3" Text="{Binding HolidayPay, StringFormat=₱ {0}}" TextAlignment="Right" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    </Grid>

                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="2.5*"/><ColumnDefinition Width="2*"/><ColumnDefinition Width="1.5*"/><ColumnDefinition Width="2*"/></Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Incentives / Bonus"/>
                                        <TextBlock Grid.Column="1" Text="—" TextAlignment="Center"/>
                                        <TextBlock Grid.Column="2" Text="—" TextAlignment="Center"/>
                                        <TextBlock Grid.Column="3" Text="{Binding BonusAmount, StringFormat=₱ {0}}" TextAlignment="Right" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    </Grid>

                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="2.5*"/><ColumnDefinition Width="2*"/><ColumnDefinition Width="1.5*"/><ColumnDefinition Width="2*"/></Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Allowance"/>
                                        <TextBlock Grid.Column="1" Text="—" TextAlignment="Center"/>
                                        <TextBlock Grid.Column="2" Text="—" TextAlignment="Center"/>
                                        <TextBlock Grid.Column="3" Text="{Binding AllowanceTotal, StringFormat=₱ {0}}" TextAlignment="Right" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    </Grid>
                                </StackPanel>

                                <Border Grid.Row="3" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource BorderBrush}" Padding="15,15">
                                    <Grid>
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="GROSS EARNINGS" Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold"/>
                                        <TextBlock Grid.Column="1" Text="{Binding GrossSalary, StringFormat=₱ {0}}" Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold" TextAlignment="Right"/>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Border>

                        <!-- RIGHT: Deductions Table -->
                        <Border Grid.Column="2" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="15,12">
                                    <TextBlock Text="🧾" Foreground="{DynamicResource ErrorBrush}" FontSize="14" Margin="0,0,8,0"/>
                                    <TextBlock Text="DEDUCTIONS" Foreground="{DynamicResource ErrorBrush}" FontWeight="Bold" FontSize="12" VerticalAlignment="Center"/>
                                </StackPanel>
                                <Border Grid.Row="0" Height="1" Background="{DynamicResource BorderBrush}" VerticalAlignment="Bottom"/>
                                
                                <Grid Grid.Row="1" Margin="15,10,15,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Description" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                                    <TextBlock Grid.Column="1" Text="Amount" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold" TextAlignment="Right"/>
                                </Grid>

                                <StackPanel Grid.Row="2" Margin="15,5,15,15">
                                    <StackPanel.Resources>
                                        <Style TargetType="Grid">
                                            <Setter Property="Margin" Value="0,6"/>
                                        </Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                                        </Style>
                                        <Style TargetType="TextBox">
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="TextAlignment" Value="Right"/>
                                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                                        </Style>
                                    </StackPanel.Resources>

                                    <!-- Editable bindings -->
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="100"/></Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="SSS Contribution"/>
                                        <TextBox Grid.Column="1" Text="{Binding Sss, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="100"/></Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="PhilHealth"/>
                                        <TextBox Grid.Column="1" Text="{Binding Philhealth, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>

                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="100"/></Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Pag-IBIG"/>
                                        <TextBox Grid.Column="1" Text="{Binding Pagibig, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>

                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="100"/></Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Loan"/>
                                        <TextBox Grid.Column="1" Text="{Binding Loan, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>

                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="100"/></Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Late"/>
                                        <TextBox Grid.Column="1" Text="{Binding Late, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>

                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="100"/></Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Undertime"/>
                                        <TextBox Grid.Column="1" Text="{Binding Undertime, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>

                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="100"/></Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Cash Advance"/>
                                        <TextBox Grid.Column="1" Text="{Binding CashAdvance, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>

                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="100"/></Grid.ColumnDefinitions>
                                        <TextBox Grid.Column="0" Text="{Binding OthersName, UpdateSourceTrigger=PropertyChanged}" Foreground="{DynamicResource TextSecondaryBrush}" TextAlignment="Left" Padding="0" Background="Transparent" BorderThickness="0"/>
                                        <TextBox Grid.Column="1" Text="{Binding Others, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                </StackPanel>
                                
                                <Border Grid.Row="3" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource BorderBrush}" Padding="15,15">
                                    <Grid>
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="TOTAL DEDUCTIONS" Foreground="{DynamicResource ErrorBrush}" FontWeight="Bold"/>
                                        <TextBlock Grid.Column="1" Text="{Binding TotalDeductions, StringFormat=₱ {0}}" Foreground="{DynamicResource ErrorBrush}" FontWeight="Bold" TextAlignment="Right"/>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- Net Pay Row -->
                    <Border Background="{DynamicResource NetPayBackgroundBrush}" CornerRadius="8" Padding="20,15" Margin="0,25,0,25">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="NET PAY (GROSS EARNINGS – TOTAL DEDUCTIONS)" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource NetPayTextBrush}"/>
                            <TextBlock Grid.Column="1" Text="{Binding NetPay, StringFormat=₱ {0}}" FontWeight="Bold" FontSize="18" Foreground="{DynamicResource NetPayTextBrush}" TextAlignment="Right"/>
                        </Grid>
                    </Border>

                    <!-- Notes / Signatures -->
                    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="15,12">
                                <TextBlock Text="📝" Foreground="{DynamicResource PrimaryBrush}" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="NOTES" Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold" FontSize="12" VerticalAlignment="Center"/>
                            </StackPanel>
                            <Border Height="1" Background="{DynamicResource BorderBrush}"/>
                            <Grid Margin="15,25,15,25">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <TextBlock Text="Prepared by: " Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBox Text="{Binding PreparedBy, UpdateSourceTrigger=PropertyChanged}" MinWidth="200" Background="Transparent" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="5,2" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                                    <TextBlock Text="Approved by: " Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBox Text="{Binding ApprovedBy, UpdateSourceTrigger=PropertyChanged}" MinWidth="200" Background="Transparent" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="5,2" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,0,0">
                        <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontStyle="Italic" Margin="0,0,15,0" Text="Click value numbers inside the table to override deductions 👆"/>
                        <Button Content="🖨  Print Section" Style="{StaticResource SecondaryButton}" Command="{Binding DataContext.NavigateBatchPrintCommand, RelativeSource={RelativeSource AncestorType=Window}}" Padding="20,10" Margin="0,0,10,0"/>
                        <Button Content="🖨  Print This Payslip" Style="{StaticResource PrimaryButton}" Command="{Binding PrintCommand}" Padding="20,10"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>

    <!-- ═══ HELP MODAL ═══ -->
    <Border Background="{DynamicResource ModalOverlayBrush}" Visibility="{Binding IsHelpModalVisible, Converter={StaticResource BoolToVisConverter}}">
        <Border Background="{DynamicResource ModalBrush}" CornerRadius="12" Padding="32" Width="500" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="📖 How to Process Payroll" FontSize="18" FontWeight="Bold" Margin="0,0,0,16" Foreground="{DynamicResource PrimaryBrush}"/>
                
                <StackPanel Margin="0,0,0,24">
                    <TextBlock Text="1. Select Employee" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBlock Text="Search and pick an employee from the dropdown menu to pull their basic salary configuration." TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,12"/>
                    
                    <TextBlock Text="2. Set Period and Days" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBlock Text="Choose the Period Start and End dates for the payslip, and input the total worked days (e.g. 11 or 13 days)." TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,12"/>

                    <TextBlock Text="3. Input Additions and Deductions" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBlock Text="Enter override values for Overtime (OT HRS), Holiday Pay, Allowances, Bonuses, and any specific Deductions." TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,12"/>

                    <TextBlock Text="4. Generate &amp; Print" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBlock Text="Review the computed details in the preview below. Click 'Print This Payslip' to finish and automatically save the record." TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>

                <Button Content="Salamat Juswa!" Command="{Binding HideHelpCommand}" IsDefault="True"
                        Background="{DynamicResource PrimaryBrush}" Foreground="White" Cursor="Hand" Padding="0,11" FontWeight="SemiBold" BorderThickness="0"/>
            </StackPanel>
        </Border>
    </Border>
    </Grid>
</UserControl>
